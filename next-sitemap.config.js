/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://bogdanarama.com', // 👈 your custom domain
  generateRobotsTxt: true,            // create robots.txt
  outDir: 'out',                      // make sure sitemap goes into export folder
};
